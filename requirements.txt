# Upper bounds are deliberate. fastmcp 3->4 and mcp 1->2 landed silently under
# open ranges and changed the streamable-HTTP client contract underneath us.
fastmcp>=4.0.3,<5
# Imported directly (mcp.types in server.py, mcp.client.* in the remote smoke),
# so declare it rather than relying on fastmcp to pull it in.
mcp>=2.1,<3
httpx>=0.28.0
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
pyyaml>=6.0
