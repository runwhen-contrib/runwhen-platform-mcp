# Upper bounds are deliberate. fastmcp 3->4 and mcp 1->2 landed silently under
# open ranges and changed the streamable-HTTP client contract underneath us.
fastmcp>=4.0.3,<5
# Declared explicitly because it arrives only via an OPTIONAL extra:
#   fastmcp -> fastmcp-slim[client,server] -> mcp ; extra == "client"
# while runtime code imports it directly (tool_trace.py -> mcp.types, loaded
# by server.py for trace_headers and ToolTraceMiddleware). The core
# fastmcp-slim dep is mcp-types, a DIFFERENT distribution that does not ship
# the mcp.types namespace. If fastmcp ever drops [client], the server breaks
# at import with nothing here having changed.
mcp>=2.1,<3
httpx>=0.28.0
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
pyyaml>=6.0
